# Cấu hình chuẩn cho Railway / Nixpacks cho dự án Node.js + Vite
[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["npm install --include=dev", "npm rebuild"]

[phases.build]
cmds = ["node node_modules/vite/bin/vite.js build"]

[start]
cmd = "node server/index.js"
